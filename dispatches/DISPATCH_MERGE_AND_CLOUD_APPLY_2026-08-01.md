# DISPATCH — Merge loyalty stacking Phase 1 + push + cloud apply

For: a Claude Code session on Mike's machine, repo `~/dev/CardCoachv2` mounted, Docker Desktop running. ~30–45 min. Gets everything done that needs Mike's machine/credentials except the mobile release (Alex) and the flag flip (founder decision — NOT in this prompt's authority).

## Prompt (paste below this line)

You are finishing the landing sequence for CardCoach's loyalty stacking Phase 1 (DATA-018 / PKG-010 / API-013 / APP-017). Read PROJECT_RULES.md and SYNC_PROTOCOL.md in `~/dev/cardcoach-docs` first — rule 10's write discipline and the CHAT SYNC block requirement apply to you. The runtime flag `loyalty_offer_stacking` ships FALSE and stays false; flipping it is a founder decision outside this session's authority. Never touch `runtime_flags`.

Work in `~/dev/CardCoachv2/mobile_app_codebase` unless stated.

1. **State check.** `git -C ~/dev/CardCoachv2 status` must be clean; `git log --oneline -4 feat/loyalty-offers-phase1` should show `cbf2739`, `89facba`, `b7f288d` on top of the base. Confirm `git branch --list 'feat/qa-009*' 'feat/ws7*'` — note any extra branches from the Cowork session for step 6.
2. **Full gate on the branch.** `git checkout feat/loyalty-offers-phase1 && pnpm install && pnpm build:contracts && pnpm verify` (the ~90-script gate; needs Docker for DB-dependent scripts). Triage failures: a failure in a file this branch touched → STOP, report, fix nothing silently. A pre-existing failure in unrelated scripts → record it, show Mike, and continue only with his in-session OK.
3. **Merge.** `git checkout main && git pull --ff-only && git merge --no-ff feat/loyalty-offers-phase1 -m "Merge loyalty stacking Phase 1 (DATA-018/PKG-010/API-013/APP-017) — dark behind loyalty_offer_stacking=false"`. Re-run the quick suite on main: `pnpm verify:loyalty-p1 && pnpm test:supabase && pnpm -C packages/engine test`. Keep the feature branch (do NOT delete) until step 5 confirms cloud state.
4. **Push.** `git push origin main && git push origin feat/loyalty-offers-phase1`. Then `cd ~/dev/cardcoach-docs && git pull --ff-only && git push origin main` (five or more local commits from the Cowork session are queued; SYNC_PROTOCOL path 1).
5. **Cloud apply — ASK MIKE FIRST, in-session, explicitly:** "Apply the two DATA-018 migrations to production Supabase now? Offers stay dark (flag false)." On yes: `npx supabase db push` against project `hrzpznlpmxxrbtwskacu`. Both migration files exist locally (rule 10(e) satisfied). Post-apply verification queries (read-only): `select enabled from runtime_flags where key='loyalty_offer_stacking'` → must be **false**; `select count(*) from offers where offer_class='loyalty_stack'` → 10; `select count(*) from offer_scope_issuers_v3 where offer_id::text like 'b0ff%'` → 7 (JOIN-guarded inserts fully populate on cloud because issuers exist there); `select count(*) from offer_scope_excluded_card_products_v3` → 3. Any mismatch → report verbatim, change nothing.
6. **Cowork-session branches.** If `feat/qa-009-golden-pack` exists: run `pnpm verify:qa-009` (or its stated verify command) and `pnpm test:supabase`; green → merge --no-ff into main and push; not green → leave unmerged, report why. Same pattern for any `feat/ws7-*` branch (docs/i18n only; run `pnpm verify:i18n-parity`).
7. **Release checklist note** (LANE CHANGE 2026-08-01: Alex stepped back — the build is Mike's; write the note to `~/dev/cardcoach-docs/dispatches/NOTE_app017_release_checklist_2026-08-01.md`, do not build in this session): APP-017 is on main after step 3; the release containing it is the activation precondition; pre-build smoke = `pnpm -C apps/mobile test` (contract-adoption tests) + the fixture-gated Maestro flow `e2e/flows/journeys/loyalty_stacking/stack_badge.yaml`; version bump per the standard EAS profile; nothing server-side needs to move first (endpoints already additive-safe). Before the flag flips: confirm build ADOPTION, not just upload — per-build installs in App Store Connect (see WORKING_NOTES #22 gate 3).
8. **Report** with: verify-gate outcome (incl. any pre-existing failures Mike waived), merge/push hashes, cloud-apply query results, branch dispositions, and a CHAT SYNC block per SYNC_PROTOCOL (you touched cardcoach-docs in steps 4/7).

Hard rules: no schema edits, no data writes beyond `supabase db push` of the committed migrations, no runtime_flags writes, no force-push, no branch deletion, no EAS build. Anything surprising → stop and show Mike.
